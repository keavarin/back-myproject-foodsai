const express = require('express');
const adminController = require('../Controller/adminController');
const router = express.Router();

//router.post('/',customerController.protect, customerController.update)


module.exports = router;